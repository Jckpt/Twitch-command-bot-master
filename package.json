{
	"name": "twitch-command",
	"version": "1.0.0",
	"description": "reads json and outputs command before their nick",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "jckpt",
	"license": "ISC",
	"dependencies": {
		"tmi.js": "^1.5.0"
	}
}
